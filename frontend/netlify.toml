[build]
  base = "."
  publish = "dist"
  command = "npm run build"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

# Environment variables are configured in Netlify Dashboard
# (Site settings > Environment variables)
# Do NOT put API keys here — this file is committed to Git
